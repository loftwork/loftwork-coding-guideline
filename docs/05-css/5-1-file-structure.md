# 5-1. ファイル構成 <Badge type="warning" text="推奨" />

原則として、ライブラリやCDNでの呼び出しを除き、1つのCSSファイルで全ページ分のスタイルを管理します。プロジェクトの要件により分割する場合は別途定義します。

- `@import` による読み込みは使用しない（読み込み速度の観点から）
- 要素への直接の `style` 属性指定は、特別な指示がない限り避ける
