# 1-3. リソースファイルのビルド（圧縮・結合） <Badge type="warning" text="推奨" />

CSS・JSのリソースファイルについて、ビルド済みのみのデータを納品する場合（ビルド前ファイルを共有しない場合）は以下を遵守します。

- クリエイター独自の記述と、外部ライブラリを使用した記述は別ファイルにする
- 外部ライブラリを使用した記述は、導入ライブラリ名・バージョンが分かるような形で結合する
- 圧縮はしても良いが、難読化（obfuscate）はしない
