# 7-4. キーボード操作

## 7-4-1. キーボードでの操作可能性（達成基準 2.1.1） <Badge type="danger" text="必須" />

すべての機能がキーボードのみで操作できるようにします。マウスでのみ操作できる実装（`onmouseover` のみのUIなど）は避けます。

## 7-4-2. フォーカストラップの回避（達成基準 2.1.2） <Badge type="danger" text="必須" />

モーダルダイアログなどでフォーカスが閉じ込められる場合は、Escキーなどでフォーカスを解放できるようにします。
