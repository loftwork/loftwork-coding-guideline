# 8-6. WYSIWYGエリアへの対応 <Badge type="info" text="任意" />

WYSIWYGエディタや複数行テキスト（HTML入力可）のパーツについては、CMS組み込み時に各種タグが挿入されるため、エリア全体を `div` タグで囲みます。

これらのパーツ内で使用されるリスト（`ul` / `ol`）や画像配置（`img`）などのタグが想定した表示となるよう、タグ自体に対するスタイル指定はおこないません。

デザインに表組みが含まれている場合は、`table` タグにclassを指定しなくてもデザイン通りの見た目になるようCSSを調整します。
